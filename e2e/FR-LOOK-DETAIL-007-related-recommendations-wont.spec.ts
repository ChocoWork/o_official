import { test } from '@playwright/test';

test.describe('FR-LOOK-DETAIL-007 関連コーデ推薦（WONT）', () => {
  test.skip('現フェーズ対象外（WONT）のため未実装', async () => {
    // WONT requirement: intentionally skipped.
  });
});
