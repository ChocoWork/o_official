import { test } from '@playwright/test';

test.describe('FR-CHECKOUT-011 消費税自動計算（WONT）', () => {
  test.skip('現フェーズ対象外（WONT）のため未実装', async () => {
    // WONT requirement - consumer tax calculation is out of scope for current phase
  });
});
